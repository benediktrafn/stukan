export default function TextLogoPlaceholder({ className = "" }: { className?: string }) {
    return (
        <span className={`font-serif font-bold tracking-widest uppercase ${className}`}>
            STÚKAN
        </span>
    );
}
